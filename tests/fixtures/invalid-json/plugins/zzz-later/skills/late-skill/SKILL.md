---
name: wrong-name
description: Fixture skill whose frontmatter name does not match its directory, reached only if the run survives the malformed manifest before it.
---

# Late skill

The plugin sorted before this one has an unparseable manifest. If that aborts the
run, this problem is never reported.
